# подключаем прометеусы

- targets:
  - "fed-prometheus-sidecar:10901"
  labels:
    cluster: fed-prometheus

# - targets:
#   - "prometheus-b-sidecar:10901"
#   labels:
#     cluster: prom-b

# подключаем gateway
# без лейблов - чтобы не создавать ощущение похожести с пунктами выше
- targets:
  - "store-gateway.example.com:10901"